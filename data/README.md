# Mobile Data Directory

Do not place full recipe corpora, JSONL preprocessing outputs, or embedding vectors here.

The mobile app should stay lightweight. Large local artifacts belong in:

```text
D:\androidCode\ChiShenMe\raw-data
D:\androidCode\ChiShenMe\rag-tools\outputs
```

Runtime DatasetPacks are downloaded by the app into its document directory on the Android device.
